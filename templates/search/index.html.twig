{# templates/search/index.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h2>Wyszukiwanie produktów</h2>

    {{ form(form) }}
    {#}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Szukaj</button>
    {{ form_end(form) }}
{#}

    {# Wyświetl wyniki wyszukiwania poniżej formularza #}
{% endblock %}
